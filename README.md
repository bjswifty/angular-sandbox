# angular-sandbox

A minimal Angular learning sandbox with two pages:

- **Home** — a simple landing page
- **Playground** — an interactive button example using Angular signals

## Run locally

1. npm install
2. npm start

Open http://localhost:4200
